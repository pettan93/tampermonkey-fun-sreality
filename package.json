{
  "name": "tampermonkey_sreality_enhance",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "npm run typecheck && node build-remote.js",
    "build:remote": "npm run build",
    "typecheck": "tsc --noEmit",
    "deploy": "npm run build && npx wrangler pages deploy build/cdn && echo. && echo Script available at: https://master.pettan-tampermonkey.pages.dev/sreality-enhance-remote.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "wrangler": "^3.78.0"
  }
}
